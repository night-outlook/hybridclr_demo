# M04 independent review record

Status: PASS. Both independent final milestone reviews accepted the complete
M04 boundary below, with no actionable findings or missing acceptance evidence.
The local four-repository tag is `assembly-shadow-m04-resolution`; the demo tag
includes this documentation-only closeout. M05 may begin after the main-agent
metadata/tag audit. M05-M07 and the overall project goal are not accepted here.

## Reviewed executable boundary

| Repository | Accepted M03 base | M04 executable source |
| --- | --- | --- |
| hybridclr | `0eca86aca3ca8e3b6f2b131ec0f2679f92deefe9` | `39eca7a2cc9c8e414f29701629da268e4e213e09` |
| il2cpp_plus | `0486098099e7e80176401267538499e611b181f2` | `229f9450c0ebe2293da2bffbfc35f160f18c69de` |
| hybridclr_unity | `0c9302ffa2f420b30774423d4da8305214a78784` | `deee300670730fbc4d864e82fa70e7b022581afe` |
| demo | `a8c15b754fe033cda9a791eb7e5cd6494b30ccfd` | `3118db3f406bdbeeacaf75737d26a89414029007` |

Demo pin-only commit `e1a0d48af6a2be136e6ff777e3628b91b1715d26` binds this
pairing. Later documentation/evidence commits do not change executable sources.
The declared [contract](M04-assembly-contract.md) requires the independent full
milestone gate even though the repository's configurable generic gate mode is Off.

## Final complete milestone reviews

Both reviewers inspected evidence commit
`1e6eeeba2adec5685ad3216343a7bdce9d82fd65` against the immutable M03 bases and
executable pairing above. They used the original emitted v2 receipts and Player
observations; the old v1 in-memory projections were excluded from acceptance.
This closeout changes only this review, the report status and contract status.
It does not change executable sources, snapshots, receipts or observations.

### Native/runtime and Player integration

Reviewer: `m03_transaction_review_gpt56sol_high_1`, independent and read-only.
Verdict: PASS, final M04 native/runtime and actual Player-integration milestone.

The reviewer independently reran the full strict v2 CLI: all ten modes passed.
It inspected context isolation, active Assembly identity, logical order and
deduplication, preserved physical diagnostics, declared references/facades and
unchanged fixed AOT indexes. P03 has 35 load/equality observations, 11 byte-bound
declared-reference rows and five executing-assembly witnesses; P01 retains
unchanged providers as AOT. Staged metadata stays private, missing closure fails
with error 11, and the external AOT consumer fails with error 14 and actual
requester/provider/index/path before metadata initialization or publication.

Actual OFF observations cover all nine disabled APIs and ordinary loading,
placeholder identity/invalidation, duplicate rejection, supplementary metadata
and caller-byte preservation. The reviewer verified every archived member and
copied artifact against originals, the four frozen hashes, and all 1,053/1,005/
987 recorded native input hashes. It inspected the native regressions, final
542-test Editor XML and recorded 160-test Python success. Independent restored
installation verification passed with 936 source/938 installed files and native
ON. Current four-repository cleanliness and the original checkout/Editor
preservation boundary also matched.

The real million-lookup measurements are 4.2434048 seconds ON and 94.4263226
seconds isolated OFF, with complete checksums and unchanged final Assembly
identity. Native adapter coverage of corner-case token conversion and runtime
failure sealing is not misrepresented as separate Player fault injection.

### Managed/package/tooling/evidence

Reviewer: `m02_integrity_review_gpt56sol_high_1`, independent and read-only.
Verdict: PASS, full assigned M04 managed/package/tooling/evidence gate.

The reviewer independently reran all ten strict v2 Player cases and restored-ON
source verification. All 29 archive members match original names, sizes and
bytes; all six copied receipts/XML match their original files. The actual final
Editor XML has 542/542 passed, 49 M04 cases and zero skips; recorded Python output
reports 160 passed. A fresh read-only recapture of the linked managed library
matches all 16 archived methods and 269 instructions exactly.

The review confirms metadata format 31, 61 native/60 linked identities, derived
native-only inventory, raw ordering, unavailable-runtime-MVID handling, closure
failures, ordinary OFF behavior and bounded benchmarks. Exact file inventory,
retained Bootstrap approvals, unique dependency edges, all four frozen hashes,
clean worktrees and original-checkout preservation were verified. No acceptance
evidence is missing within this scope. The reviewer explicitly permits a
documentation/status-only closeout and authorized local tags without a new
executable review when reviewed sources/artifacts remain unchanged and the main
agent performs the final metadata/tag audit.

### M05 entry decision and residual limits

Both companion gates PASS. The accepted executable source pairing remains the
table above; only the demo documentation closeout follows the reviewed evidence
commit. Main-agent audit must confirm that exact metadata-only delta, unchanged
paired heads and artifact bytes, clean worktrees, and matching local tag targets.
No push or PR is part of this milestone.

Acceptance is pinned macOS ARM64 Unity 2022.3.62f2 IL2CPP. Evidence is unsigned and
locally bound, runtime MVID remains unavailable, and callback coverage is limited
to known-name noninvocation plus the disclosed native/wrapper checks. Single
controlled name-load timings are not production-frame or managed-GC claims.
Complete Type/Member caches, execution semantics and Unity asset integration
remain M05-M07. Those limits do not waive any later milestone requirement.

## Bounded source-readiness reviews

The independent native/runtime reviewer found no actionable issue in the
explicit resolver contexts, physical lookup split, stable logical enumeration,
semantic AssemblyRef authorization, declared identities, first-error sealing
and preserved physical indexes. The reviewer inspected actual harnesses and
rehashed their recorded inputs. That PASS preceded actual Player validation
and was explicitly not milestone acceptance.

The independent package/managed reviewer found no actionable issue in literal
AssemblyName/path acquisition proof, exact Bootstrap approval, separate M04
probe/build/replay contracts, or narrow shared M03 helper extraction. A separate
corrective review accepted removal of the redundant runtime graph edge while
preserving exact call-site approvals and the actual-project policy test.

## Native metadata corrective-source review

Reviewer: `m02_integrity_review_gpt56sol_high_1`, read-only independent review.
Exact correction: demo `a0dcb5126d6ce9109d338ca0e2499ddf30d05ca2` through
`3118db3f406bdbeeacaf75737d26a89414029007`.

Verdict: PASS, corrective-source readiness only; no actionable findings.
The reviewer independently inspected the strict C#/Python evidence contracts,
actual format-31 ON/OFF metadata, old-receipt rejection, source diff and fresh
Editor XML (542/542, 49 M04 cases, zero skips). All 60 linked identities matched
the 61-row native inventory. The extra native assembly was derived at table/
image index 60 with token 536870912; no name-based exemption was added.

The review preserved the distinction between a pre-registered placeholder's
physical slot and public logical enumeration. Runtime precommit-use protection
and the no-Shadow-API OFF benchmark remain unchanged. Native metadata contains
no MVID; unsupported runtime module MVIDs are not filled from expected artifacts.
Existing v1 receipts remain untouched and rejected by the corrected schema.

This review was an immutable-source gate only. Its in-memory v1 projections
were diagnostic checks, not emitted-v2 receipt or runtime acceptance.

## Final gate requirements

Final reviewers must inspect the committed evidence boundary and actual raw
artifacts, not accept these summaries alone:

- Complete ten-mode v2 ON/OFF Player matrix and strict full CLI verification.
- Actual build GUIDs, native libraries, metadata files, linked DLLs, fixtures,
  baseline/resource bytes, source pins and independent Editor replay.
- Exact logical versus physical inventory/order, active identity and declared
  references; staging and closure rejection without fallback.
- Ordinary OFF loading, placeholder, supplementary metadata, duplicate error
  and explicitly bounded callback behavior.
- Million-lookup Player measurements, separate zero-allocation native evidence,
  preserved M03 native/visibility checks, final Editor/Python tests.
- Restored native ON, frozen M00/M01 bytes, four-repository scope audit and
  preservation of the original demo checkout and pre-existing Editor.

Limits remain explicit: the native harnesses use disclosed metadata/exception
adapters; unsigned receipts establish consistency, not cryptographic
authenticity; missing-name native boundaries are not inferred solely from
bodyless managed icalls; full Type/Member cache, execution and Unity asset
integration remain M05-M07. Host validation is pinned macOS ARM64 IL2CPP only.
