# R00 delivery

Status: **R00 accepted after independent stage review PASS at `2eb4a36`. R01 may start. H1 has not been reached.** The [review verdict](continuation-code-review.json) grants no human-gate or release approval.

## Pairing and workflow

The fresh workflow completed on Unity `2022.3.62f2`, macOS `StandaloneOSX` arm64, with baseline identity `M07-Baseline-R00-v1`. The demo metadata HEAD before this evidence commit is `e2cc808d49b0d78d73b5bde79f15405e5bdf1a5b`; the executed source pin is `a4f3ce6ac271ddf37d4ea2c3a06fbdc402098e59`. The native and package revisions are unchanged:

| Repository | Revision |
| --- | --- |
| hybridclr | `a19db144751f4f016769b90e61a80b8c27578678` |
| hybridclr_unity | `2180b99daf39095cd76301da2bdf34ac945ee8b4` |
| il2cpp_plus | `666f5f4bfa3aa408e6ba4c3552fc4758dcc6bcc8` |

The workflow receipt is `m07-build-workflow.json` with SHA-256 `7cb240a109e23153f8b3a3e0129cba5e6c69d267984a5cae8f7e865675a5cc97`. It binds the native ON/OFF Players, resource baseline, fixture manifest, Editor replay receipt, and project-settings hash. The current strict M07 gate is `R00-M07-current-pair-v4/strict-gate.json` with SHA-256 `58efa4d71bbb12d6c7746feee50a8def4cbb5e9d7297ff2bdc48de98c841f6ea`.

## Evidence delivered

- The complete current-pairing M07 matrix passed strict verification: all fourteen modes passed, including P01, P02, P03, P04, rebuilt P05, and feature-off mode. No modes were missing and no incomplete/skip override was used.
- The four R00 worlds passed strict verification: `R00-ON-NoPatch`, `R00-ON-P01`, `R00-ON-P03`, and `R00-OFF-NoPatch`. Each fresh process recorded first invocation, 100 warmup, 10 repeated, and 10,000 repeated observations for managed allocation, reflection invocation, and closed-generic execution. Allocation rows record actual constructor increments and checksums. The performance strict-gate SHA-256 is `74d57978c02932116ec992d83c1618184f9e640fded14af28fb87cce8123452a`.
- Performance observations are diagnostics. The sample count is one process per world; timings include reflection entry and managed argument boxing, and readiness is configured witness resolution from the parent launch boundary, excluding business resource and scene load. The file-handle duplication API is unsupported on this platform, so Player process-start time is unavailable. The Player records invocation/ready UTC timestamps; parent launch-to-readiness timings use the parent process-launch boundary.
- The Python source-stability suite passed 365 tests with zero skips. Current Editor checks passed in separate invocations: 278 demo tests and 650 package tests, 928 total with zero failures or skips. The earlier full Editor rerun and initial native suites remain retained evidence. The native suites record M03 identity/name/facade/OFF checks, M03 visibility checks, M04 checks, M05 checks, and M06 checks; they do not become a fresh full M05/M06 Player matrix.
- Three bounded shutdown repetitions completed successfully for P05 and feature-off. One separate ASLR-enabled LLDB run observed the expected post-release destruction ordering before quit and physics cleanup. It is diagnostic ordering evidence, not proof of a global Unity shutdown-race fix.
- Native generic evidence found the closed `Int32` generic symbols and AOT reference in the generated ON Player. Its capture-time `runtimeExecution` field remains `NotRun`. Later strict R00 results separately prove closed-generic execution in all four worlds; the symbol receipt alone does not.
- Native proof-build, metadata-row-scan, and runtime-allocation counters remain explicitly unavailable. Constructor counts establish payload construction for the witness, not total managed or native heap allocation. No GC heap delta is used as allocation proof.

## Corrective history and retained failures

The chronology remains part of [R00-continuation.md](R00-continuation.md). The material observations are retained as follows:

- `R00-OBS-003`: the first build-v2 resource authoring call regenerated eight tracked baseline inputs. The process was stopped before a Player was produced; the regenerated bytes and hashes were retained, and only those inputs were restored to their pinned bytes.
- `R00-OBS-004`: build-v3 exported an Xcode project because interrupted strip-only work left persistent `createXcodeProject` and `buildScriptsOnly` settings. Capture rejected the output for missing `GameAssembly.dylib`. The M07 build now scopes both settings around the actual Player build, restores them in `finally`, and has success/throwing-delegate tests for both original states.
- Earlier review fixes corrected readiness timestamp capture, added the exact compiler dependency declarations for the new reflection sites, separated authoring from read-only source validation, corrected the exact scene scalar predicate and UnityEvent sequence parsing, and added the executable-build settings guard. Independent re-review of these corrections returned PASS. These review results do not replace the final R00 stage review.

## Limits and follow-up

The performance result is a bounded observation, not a statistical performance baseline or Release claim. It uses one Development IL2CPP process per world and has no production-scale workload. The fresh current-pairing run does not rerun the complete historical M05/M06 Player matrices; the R00 process includes the affected M06 `new`, `dispatch`, and `generics` regressions, while the native M05/M06 suites remain their own evidence. Runtime capacity boundaries and large closure support remain R01 work. Windows and Android remain unrun.

The shutdown ordering trace used LLDB breakpoints and one instrumented P05 run; it can show the observed order but cannot establish absence under every workload or prove the historical SIGSEGV cause has been fixed. The historical crash and failed initial matrix remain unchanged evidence.

## Review gate and rollback

The independent disposition is **PASS**, with no remaining actionable findings. The reviewer independently recomputed both strict gates and checked the raw results, archive members, claims, source pairing and file inventory. R01 may now start. H1 occurs only after R01 and any required R01B, where work must stop for human complete review. No human gate or release approval has been granted.

Rollback is to withhold promotion and keep the current Players and raw evidence retained. The original repositories and historical receipts remain untouched. Any source-pin rollback, archive decision, or commit publication is owned by the root integration task; this documentation update makes no such mutation.

## Evidence index

The continuation archive and its hash-bound raw paths are indexed in [continuation-evidence-index.json](continuation-evidence-index.json). [current-snapshot-scale.json](current-snapshot-scale.json) and [continuation-changed-files.json](continuation-changed-files.json) record the final snapshot-scale and changed-file boundaries. The direct raw artifacts used for this draft are:

- `_temp/AssemblyShadow/M02Validation-970e9107f1e7483a84abaddfac31e0d2/m07-build-workflow.json`
- `_temp/AssemblyShadow/R00-M07-current-pair-v4/strict-gate.json`
- `_temp/AssemblyShadow/R00-performance-current-pair-v4/strict-gate.json`
- `_temp/AssemblyShadow/R00-P05-ordering-v4/ordering-summary.json`
- `_temp/AssemblyShadow/R00-native-generic-ON-v4/evidence.json`
- `_temp/AssemblyShadow/R00-shutdown-repetitions-v4.json`
- [performance-observations.json](performance-observations.json)
