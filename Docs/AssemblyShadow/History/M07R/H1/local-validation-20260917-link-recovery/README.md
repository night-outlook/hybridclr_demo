# H1 Local Validation — M07 link recovery and downstream attempt

This checkpoint preserves fresh evidence for source anchor `68df00fe31a199491b313cc17f25575663b7452b`. The normal M07 workflow passes Native ON/OFF, structural fixture finalization, Editor replay, generated `link.xml` recovery, and exact three-file success restoration.

Downstream acceptance is blocked before startup11 Player launch because the fresh real-Unity `AssemblyShadowDemo.Bootstrap.dll` uses a `#-` metadata stream with 1,675 `MethodPtr` rows. The strict M05 raw-admission verifier rejects all pointer tables. Local did not broaden that semantic contract.

The 8,192-entry 512 MiB capacity corpus was recovered and every hash verified, but capacity, lazy/dense, old-Player, and performance acceptance cannot form a fresh chain. The cleanup archive also lacks the two sealed dense adjunct DLLs even though it retained their manifest and historical PASS audit. Independent native M03–M06 and R01B checks passed except the unavailable dense parser case.

`raw-evidence.tar.gz` is authenticated by `results-summary.json` and `MANIFEST.sha256`. Historical checkpoints retain their original identities. H1 remains in progress; M08 is not PASS; R02 was not started.
