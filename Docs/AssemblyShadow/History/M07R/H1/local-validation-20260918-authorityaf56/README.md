# H1 Local Validation — authority `af56b841` V04 return checkpoint

This checkpoint preserves the fresh Local chain from candidate checkout `66b4b86570bce92e65e99ba795eb9c4f72582c8c` and source anchor `af56b841e9ae80be0b1748e546338f9b68da2717` on Unity 2022.3.62f2 / StandaloneOSX arm64.

V00–V03 passed with exact candidate/reproduction authority and fresh build provenance. Controlled M07 reached its deliberate failure and restored all guarded inputs. Separate normal M07 baseline `M07-Baseline-H1-AuthorityAf56-Normal-20260918A`, startup11, and strict M07 Gate 3B 14/14 passed.

The repaired failure launcher proved the new earliest-admission contract in three real Players: distinct schema-v2 bindings and Baseline capsules, successful same-PID early receipts, exact commands, late same-PID results, logs, and unchanged hashes across 17,275 inputs. Acceptance still failed because the late probe hard-codes native budget capability profile 1 while the current baseline/patches use profile 2. All three modes stopped at `Selected patch is not the complete baseline-bound budgeted closure`; strict verification returned `Failed`.

Independent continuation produced fresh PASS evidence for ordinary and mixed 512 MiB capacity, deterministic dense-v2 parser/FieldRVA coverage, generic/index/cache/attribute/live-capability coverage, and retained M03–M06/R01 native suites. Lazy Player coverage failed before launch because its verifier requires the unavailable historical dense schema-1 inventory while the current generator emits schema 2. Old-Player rejection and controlled paired performance are `Unavailable / NotRun` because the listed checkout lacks their immutable old/reference build graphs. Nothing was reconstructed or promoted.

`raw-evidence.tar.gz` contains 901 evidence files. Large live Players, compiler/resource trees, and corpora remain in the validation workspace and are hash-bound by archived strict receipts and full before/after maps. No cleanup occurred. H1 remains `InProgress`; M08 remains historical `FAIL`; `humanGatePassed=false`; `mayEnterR02=false`.
