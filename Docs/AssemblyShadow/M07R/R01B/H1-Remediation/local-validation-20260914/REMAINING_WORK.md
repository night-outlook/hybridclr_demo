# Deferred work and next acceptance steps

Larger changes are listed here rather than implemented, as requested. H1 remains blocked; no human approval or R02 entry.

- [ ] **D01 / L02: Diagnose and fix Bootstrap ILPP emission.** Reproduce `Expected a null constant` from the retained failed Player-script outputs identified in `editor-tests.log`. Obtain a full exception stack and identify the offending metadata/PDB constant. Inspect `hybridclr_unity/Editor/AssemblyShadow.CodeGen/ReflectionBindingTransformer.cs` module.Write and the ILPP exception handler. Do not guess a C# source workaround, discard symbols, or relax exact-site authorization. If the package serializer needs changes, treat this as the deferred implementation scope. Verify both `M05EditorEvidenceTests.LayoutFixtureUsesPublicPlayerCompilerWithUnchangedSerializedFields` and `R01FailureFixturesTests.InitializerThrowCompilerShapeIsIsolated`, then actual current witness variants/guards. Raw local DLL/PDB attempts remain under `_temp` at the exact paths in the log; they are not transferred as Git binary artifacts. Reproduce in the next environment if unavailable.
- [ ] **D02 / L01: Integrate H1 parser at its owner.** The separate wrapper parses six sites, but normal M02–M07 import paths still reject the current config. Preserve five-site historical semantics, exact six-site method/image/provider authorization, and reject extra/duplicate sites and tampered variants. Test actual normal CLI/import paths; do not merely change test imports to the wrapper.
- [ ] **D03: Restore/authenticate historical M01 bytes or explicitly disposition their unavailability.** `BaselineArtifacts/StandaloneOSX/M01-Baseline-v1` is absent. Three Python cases and one Editor case depend on it. Do not manufacture or relabel historical evidence. One additional full-Python skip is retained by exact ID in the inventory.
- [ ] **L00/L03: Freeze fresh matching sources/configuration before six builds.** Existing source pins deliberately remain historical: demo `9c6b812`, frozen native `db685e4`. Full installed verification fails on demo build-input changes, while native-only inspection passes with `demoSourceVerified=false`. This is not current-candidate proof. Freeze current matching candidate native/package/IL2CPP and separate unfixed native source; create fresh pins/receipts without modifying old receipts. Candidate four modes and reproduction ON Debug/Release must use the managed wrapper and verify direct graph provenance (or explicit reverified route B).
- [ ] **L04–L08:** Fresh eight unfixed cells, candidate 132 cells, baseline/fixtures/replay and all eleven startup modes, affected M06/M03–M07 capacity and lazy/dense/FieldRVA boundaries, and controlled performance pairs/equivalence as specified in `../continuation-20260914/LOCAL_VALIDATION_TASKS.md`.
- [ ] **L09–L11:** Fresh inventories and mandatory successor membership/archive/index; independent whole-chain M08 PASS; then STOP for explicit human H1 approval in the user's other environment. Do not begin R02.

Storage was initially 1 GiB (below the historical 10 GiB preflight request), later 33 GiB. It is no longer the final blocker; recheck before builds. No cleanup of historical evidence was performed. Broad builds were not started after the concrete L02 compiler failure and independent review findings.
