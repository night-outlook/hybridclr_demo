# D01/D02 local validation checkpoint — 2026-09-14

**D01 and D02 local integration passed. H1 remains `InProgress` and technical readiness is `Blocked` at L03 compiler provenance. Whole-chain M08, human H1 approval, and R02 remain closed.**

The unpublished `H1_D01_D02_20260914` delivery was rendered into a separate review directory before integration. All 44 delivered files matched their declared base blobs and payload hashes. The delivery's 53 portable tests passed, but they are classified only as `PackagingToolTests`; they are not Unity or Player acceptance.

The package change now preserves and audits Portable PDB constants through IL post-processing, records full failed compiler captures, resolves explicit Unity compiler references, and canonicalizes project-relative reference paths. The demo change integrates the strict six-site H1 form into the normal M02 result owner and CLI while retaining historical five-site parsing. The final fresh diagnostic batch passed all eight tasks: D02 owner 15/15, normal CLI, PDB fixtures 20/20, both focused compiler fixtures, full Python, and full Unity Editor tests. Exact inventories record 1,067/1,067 Editor tests passed and 758 Python tests passed with one declared skip requiring explicit real compiler/config paths.

Six preserved compiler captures were replayed through unresolved, compiler-reference, and resolved-enum modes. All six PDB audits and all six replay tasks passed. The two affected unresolved captures retained the exact `Expected a null constant` failure on `System.Reflection.BindingFlags memberFlags`; their compiler-reference and resolved-enum routes produced exact constant inventories. Route A, explicit compiler references, is selected. The derived captures bind the original raw capture hashes but remain replay diagnostics, not Player-build provenance.

Candidate and reproduction Unity compilation passed after installing their exact pinned package/native sources. Strict installed-runtime verification passed for both checkouts. The candidate source pins are native `1d2df7c`, package `0ea633a`, IL2CPP `6be7f38`, and demo source `87011e5`; the reproduction pins are native `99cdb1b`, the same package/IL2CPP commits, and the preserved unfixed demo source `c8d2faf`. The separate performance-reference checkout remained at `88508b5` and was not modified.

The first valid L03 attempt, candidate feature-on Debug run `fb64c3133d0243f79f096f85fc3143aa`, completed the Unity Player build and sealed build GUID `993780fe71f2412fa4c11456d5124599`, input snapshot `6cea2b1755bbb3a4873e708de105dc741af8e32da15dd8e7b655bf4f935e0536`, and GameAssembly SHA-256 `619e61ae9e32082d1dd157e9fbfd19342fa384d6732bc4b9440b8b4df9ee10e7`. Compiler provenance then failed with `Forced preprocessor inputs require explicit preprocessing evidence`; no build receipt exists. Restoration succeeded, and the two generated working-tree changes were separately captured before being restored to the committed bytes.

The blocking graph has 446 native compiler actions: 444 object compilations and two PCH producers. Of the object actions, 307 consume `-include-pch`. The safe correction must bind the exact PCH bytes, producer action, source header, arguments, dependencies, and independently verified effective feature macros. Merely allowing `-include-pch` would weaken the provenance boundary and is rejected. The detailed bounded scope is in [PCH_PROVENANCE_BLOCKER.json](PCH_PROVENANCE_BLOCKER.json) and [REMAINING_WORK.md](REMAINING_WORK.md).

Historical M01 baseline bytes were restored only after all 93 files were checked against the committed R00 fixture index. They are classified `ReusedAuditedImmutableHistoricalEvidence`, not fresh evidence. Initial diagnostic failures, the invalid build-ID attempt, the failed provenance build, the original raw captures, and every replay output remain intact. Portable copies of the critical logs and receipts are under `evidence/`; [external-evidence-roots.json](external-evidence-roots.json) binds the complete local trees, including the 534 MiB failed build root.

Resume from [NEXT_AGENT_PROMPT.md](NEXT_AGENT_PROMPT.md). Do not run the other five builds until the PCH provenance correction passes focused positive and tamper tests. Do not promote the successful Player-build phase because the provenance phase failed, do not seek human H1 approval without an independent whole-chain M08 PASS, and do not begin R02.
