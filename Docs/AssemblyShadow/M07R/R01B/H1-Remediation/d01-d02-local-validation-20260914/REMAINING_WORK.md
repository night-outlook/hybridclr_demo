# Remaining H1 work after D01/D02

The first remaining item requires a substantive provenance change, so it is recorded here instead of being implemented during this local-validation run.

- [ ] **L03: support PCH-backed compiler provenance without weakening forced-input checks.** Retain and hash both generated PCHs; bind each to its exact Bee producer action, compiler, arguments, source header, input set, output, and object-action dependency; prove the effective tracked macro state after PCH application; admit only the exact evidenced `-Xclang -fno-pch-timestamp` pair; continue rejecting arbitrary `-include`, `-imacros`, `-Wp,`, `-Xpreprocessor`, and `-Xclang` routes. Add positive and tamper tests for PCH bytes, producer/input drift, dependency drift, feature-macro drift, and forbidden arguments. Re-run candidate feature-on Debug first and require a complete build receipt before continuing.
- [ ] **L03: complete six fresh provenance-bound builds.** After the first build passes, create candidate feature-on/off Debug/Release and unfixed reproduction feature-on Debug/Release builds from the published pins. Preserve each new immutable root and receipt. Never reuse the failed `fb64c...` output as acceptance evidence.
- [ ] **V05 and L04–L08: complete runtime evidence.** Verify each sealed build against its source and compiler provenance, then collect the eight unfixed cells, 132 candidate count cells, normal controls, exact reproduction failures, all eleven startup modes, capacity/lazy/dense/FieldRVA boundaries, and controlled performance pairs. Keep `Passed`, `Failed`, `Unavailable`, `NotRun`, and `NoCoverage` distinct.
- [ ] **L09: seal the successor package.** Build the new successor directory, archive, evidence manifest, exact inventory, source/build binding, and membership/index updates. Preserve every historical v7–v11 directory and all earlier failed attempts.
- [ ] **L10–L11: obtain independent whole-chain M08 PASS.** The reviewer must inspect the entire fresh chain, including build provenance and successor membership. Only a genuine PASS may make the package ready for explicit human H1 approval. Stop at that human gate. Do not begin R02.

The exact graph facts and required correction are in `PCH_PROVENANCE_BLOCKER.json`. The complete failed-build root is `/Users/ah/GitHub/hybridclr/assembly_shadow_h1r/hybridclr_demo/_temp/AssemblyShadow/H1CountBuild-fb64c3133d0243f79f096f85fc3143aa`; its aggregate hash is recorded in `external-evidence-roots.json`.
