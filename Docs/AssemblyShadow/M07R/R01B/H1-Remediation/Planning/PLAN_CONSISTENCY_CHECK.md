# v2.0 文档一致性检查

日期2026-09-10。仅检查本计划文件/模板；不是新source review、runtime测试、原archive认证或人工H1结论。

## 内容核对

用户1A+2A与四项finding闭环保留；14×3×2=84基础cells、附加variants、original 8k/mixed/bytes/25%、全部相关mode/test覆盖及每模式10对性能样本未缩减。

协调者全局规格与执行者单卡分开；inspection/implementation/build/validation/review/文档动作准确命名和分别授权，不用单条指令自动运行全部milestones。只有H1是此工作包必需人工放行点，普通卡返回协调者不新增重复策略询问。

必要raw/context不被措辞精简删除。Stage/Abort/Patch/FieldRVA及错误状态保留；用户notes原字节存档，实际执行规则不以改词规避为目标或保证交付。

M00只读首任务与协议登记分开；M01历史缺项不阻塞无关独立开发，但仍阻止最终完整H1。新运行实际所需输入必须就绪。M05测量准备早于source freeze；OFF观察不启用Shadow；等价更早生产count guard可以有效覆盖目标，非count错误是NoCoverage。

source/build/fresh/reuse/外部artifact/冻结status严格分开；原review/status字节不改。协议、schedule、archive、index与后继review不构成循环hash。H1模板保持null/false，文档提交不产生runtime候选或放行。

## 机器检查范围

已运行本地文档检查：UTF-8/LF、JSON可解析、相对文件链接、十阶段ID/路径及无环依赖、初始Pending状态、关键数值/原pins、引用附件字节hash、生成的文件清单和Git blob/tree hashes。

此检查不抓取每个外部链接，不验证GitHub原86MB archive，不运行Unity/Player/native/项目Python测试。提交后另核实际commit、分支和新增文件集合；只有计划文档应出现在diff中。
