# Remaining local validation — not completed in the chat environment

H1 remains InProgress / technically Blocked; historical M08 FAIL remains authoritative. Every row below is pending unless supported by newly captured local results. This is not a request for premature human H1 approval.

| ID | Required local work | Required result/evidence |
|---|---|---|
| V00 | Review and integrate the exact D01/D02 changes; confirm branch, package selection and unrelated working-tree state. | Actual source diff and source identities; no historical evidence edits. The target remote branches were not changed by this delivery. |
| V01 / D02 | Run `test_h1_m02_owner.py` through the complete normal owner and `verify-m02-results.py` CLI. | 15 fresh tests plus existing relevant M02–M07 tests. Five-site history still parses; six-site exact authorization and runtime guard fields are enforced; no wrapper installation is needed. |
| V02 / D01 | Compile the package and both candidate/reproduction Editors; execute all `ReflectionPdb` tests. | 19 NUnit tests, exact XML IDs, Unity/dnlib versions and logs. Resolve real API/assembly-reference issues rather than counting static inspection as compilation. |
| V03 / D01 | Execute `M05EditorEvidenceTests.LayoutFixtureUsesPublicPlayerCompilerWithUnchangedSerializedFields` and `R01FailureFixturesTests.InitializerThrowCompilerShapeIsIsolated`. | Both pass on the actual Player-script compiler. Retain every failed PE/PDB attempt and full exception first. Actual local D01 root cause is not yet established by this delivery. |
| V04 / D01 | Run unresolved/A/B replay and raw-PDB audit on every newly retained failing input as needed. | Source-specific explanation, exact tool/input hashes, unchanged constants and scope bytes, existing MVID/sequence-point/template checks; record A/B selection. No automatic fallback or symbol loss. |
| V05 | Rerun normal M02 witness runtime evidence after a real new Player build. | Actual emitted current guards reject null/tampered bytes, leave caller bytes unchanged and trigger no extra AssemblyResolve. Configuration-only CLI output cannot satisfy this row. |
| V06 / D03 | Locate and authenticate the historical M01 inputs, or explicitly record their unavailability under the governing review process. | Original bytes and provenance, not a newly generated replacement labeled historical. Keep missing evidence distinct from a failed implementation test. Preserve exact skip/nonpass IDs. |
| V07 / L00–L03 | Freeze fresh matching source/configuration sets across the four repositories and separate unfixed reproduction. | Clean exact identities; installed-source verification with demo source actually verified; fresh pins/receipts. Review any importer `.meta` changes before freeze. Inspect actual managed capture timing/defines/configuration and native compiler/response-file provenance. |
| V08 / L03 | Four candidate count builds (ON/OFF × Debug/Release) and two reproduction ON builds (Debug/Release). | Fresh prepare/build/restore processes; documented managed-provenance wrapper; actual current source-to-DLL/native chains. Restore settings after success or failure. Do not silently reuse old builds. |
| V09 / L04–L06 | Run eight unfixed reproduction cells and the current 132 candidate count cells; rebuild current baseline, fixtures and replay; run all 11 startup modes. | Raw results bound to exact builds/inputs, including negative-mode expected behavior and current schema. Earlier summaries are not substitute evidence. |
| V10 / L06–L08 | Complete affected ordinary/mixed capacity, 8,193 rejection, ≥25% usable encoded headroom, lazy/dense/FieldRVA and M03–M07/OFF regressions; complete controlled comparable Development performance pairs. | Preserve original workload and 1A+2A acceptance. Keep the old performance profile genuine. Reuse only explicitly audited unchanged evidence; report unavailable/failed/NotRun distinctly. |
| V11 / L09 | Produce fresh complete test-ID inventories and seal the successor archive/index with mandatory raw dependencies. | Recomputed hashes, membership, source/build/launch links, all new capture/audit artifacts needed to interpret the results. Old archives and frozen statuses remain unchanged. |
| V12 / L10 | Independent design → plan → four-repository implementation → raw whole-chain evidence review. | Genuine M08 PASS on exact final source/evidence versions, with findings closed or normatively dispositioned. Diagnostic unit tests and this source preparation are not M08. |
| V13 / L11 | Stop and request the user's H1 decision. | Only after V12 passes. `humanGatePassed` cannot be set by a tool or agent; no R02 before explicit user approval. |

Use the committed historical plan and transfer records to resolve detailed acceptance requirements. This list adds D01/D02 work; it does not replace or relax the earlier H1 requirements.
