# Local Validation report

## Current run — 2026-09-20/21 authority `69130bbb`

### Exit

**Local Validation → Primary Implementation: RETURN REQUIRED after V04 performance pilots**

Fresh V00 authenticated candidate checkout `4424a920b7d25880eb78aa2e0d9b0cd9662dff84` and exact source anchor `69130bbb3a6df516916dddb5ad263799a7c6e5e3`. Candidate authority returned `SourceTargetVerifiedNotBuildAccepted`; reproduction tooling, the exact protected profile-1 family, and both installed runtimes passed.

Both controlled-workflow boundary repairs passed in real Unity/IL2CPP. Protected profile-1 and current profile-2 each produced complete graph-bound Development Native ON/OFF Players, exact input restoration, fixture/replay receipts, and passed controlled evidence. Current native provenance ran with `verificationEnvironmentScope=NativeOnlyWithoutOuterM07WorkflowAuthority`; the following outer M07 authority checks passed. Old-Player rejection, strict build-map freeze, unchanged preregistration, and all four pilot pairs also passed.

Formal sampling did not complete. The first formal invocation remained in strict re-verification of the four pilot graphs for 47 minutes 39 seconds before creating any formal output root or Player. Completing forty formal pairs through this path projects beyond a bounded Local batch. The outer verifier was terminated before formal launch, with no child process. Formal performance is therefore `Unavailable`; no performance acceptance or H1 acceptance is claimed.

### Results

| Cell | Result | Evidence / disposition |
| --- | --- | --- |
| V00 candidate authority | `Passed` | Exact checkout and `SourceTargetVerifiedNotBuildAccepted` for `69130bbb...` |
| V00 reproduction/protected refs | `Passed` | Reproduction tooling bound to current anchor; protected demo/native/package/IL2CPP commits exact |
| V00 installed runtimes | `PassedAfterCandidateReceiptRefresh` | Candidate and protected runtime source/inventory verification passed with Shadow ON; final receipt hashes remained `997ce2d3...` and `54bfe84c...` |
| V01 complete Python inventory | `FailedGeneratedPrerequisite` | 1,018 total: 970 passed, 28 explicit skips, 19 errors, 1 failure; all 20 nonpasses are the M02 evidence class missing the ignored historical M00 fixed image |
| V01 bounded Primary | `Passed` | 326/326 |
| V01 direct handoff/R01/lazy | `Passed` | 11/11, 7/7, 20/20, 20/20, 16/16, and 10/10 |
| V01 PowerShell recovery regressions | `Passed` | Generated-input labels plus success/failure exact two-input recovery |
| V01 broad Unity EditMode | `FailedGeneratedPrerequisite` | 1,076 total: 1,066 passed, 9 failed, 1 inconclusive; failures are missing clean-owner M00/M01/M05 generated prerequisites; current source-contract assertions passed |
| V01A source delta audit | `PassedExpectedScope` | `316894a8...` to `69130bbb...` functional delta is the workflow/tool/test recovery and child-environment repair; no native transaction or performance protocol change |
| V02/V03 authority/runtime boundary | `PassedScoped` | Candidate/protected runtime checks passed; direct top-level `--skip-demo-source` under outer M07 authority was still rejected |
| V04 profile-1 controlled graph | `PassedFresh` | Baseline `M07-Baseline-H1-Perf-Reference-6913-20260920A`; fixture/replay plus real Native ON/OFF Players and controlled evidence passed |
| V04 profile-2 controlled graph | `PassedFreshAfterRetainedAttempts` | Missing-M00 attempt and fresh-hash mismatch attempt retained; authenticated policy-pinned M00 input staged; baseline `M07-Baseline-H1-Perf-Current-6913-20260920C` then passed completely |
| V04 exact stage restoration | `PassedFresh` | ON/OFF `link.xml` and `ProjectSettings.asset` original/restored hashes match for both graphs; following outer authority guards passed |
| V04 nested native scope | `PassedFresh` | Current Native ON/OFF evidence records `NativeOnlyWithoutOuterM07WorkflowAuthority`, exit 0, empty verifier stderr |
| V04 old-Player rejection | `PassedFreshAfterRetainedCliAttempts` | Correct receipt-bound attempt passed with expected Player exit 1 and `inputsUnchanged=true`; two earlier argument-shape failures retained |
| V04 strict build-map freeze | `ComparabilityPassed` | Frozen map matches Unity, arm64, stripping, codegen, startup, measurement core, and witness; runtime ABI/source pins differ as required; acceptance not claimed |
| V04 preregistration | `Passed` | Protocol bytes unchanged, schedule semantic fields unchanged, 44 frozen pairs |
| V04 pilots | `PassedFreshWithRetainedRetry` | Four scheduled pilot pairs passed; `R00-ON-NoPatch` attempt 1 timeout retained, whole-pair attempt 2 passed |
| V04 formal / analysis | `Unavailable / NotRun` | Formal pair 1 stopped before launch after 47:39 of repeated pilot verification; zero formal samples; no analysis |
| V05 / independent M08 | `Ineligible / NotRun` | Mandatory formal performance and analysis absent; complete Python/EditMode inventories also retain nonpasses |

### Controlled graph facts

Profile-1 build GUIDs are `78936bf024434cb8b328f40ce7dd97bb` (ON) and `5722f1a804bc45d096eedc58bb18b081` (OFF). Profile-2 build GUIDs are `7e4d6b8717324b20bc57b06b9d9eebad` (ON) and `02f5e40f1f7540b19a9674e9f32511c8` (OFF). All four controlled evidence files report `result=Passed` and `provenanceComplete=true`.

The current workflow's first attempt failed before Unity because the clean owner lacked the ignored M00 fixed input. A fresh M00 build generated SHA-256 `c98b...`, which correctly failed policy validation against declared SHA-256 `9108a239...`. The fresh bytes and failure are retained. The exact declared bytes were then staged from the already authenticated protected profile-1 family, with source/destination/declared hashes recorded, and the successful current graph was regenerated. This staging is an authenticated immutable input operation, not relabelled build evidence.

### Performance facts

The map-freeze receipt reports `result=Passed` and binds protocol `H1R-Development-Paired-v1`. Common measurement core SHA-256 is `d53d566a48c5af46d2ad0ca84d486a1703cd6f10664afba5d8cde31859ac9ea0`; witness SHA-256 is `c395b12191fafd0d8dd3ec935f1e48829a16fab88b1e78eec48ab3c7ad96af33`.

All scheduled pilot modes passed: `R00-OFF-NoPatch`, `R00-ON-NoPatch`, `R00-ON-P01`, and `R00-ON-P03`. The retained failed pilot attempt used a locally selected 300-second timeout, timed out side A, could not prove the owned group gone at cleanup, and correctly skipped side B. No residual process existed when audited. Attempt 2 used the protocol's 900-second bound and passed both sides.

Formal pair 1 used the completed cumulative pilot index. Before any formal side output existed, the driver re-ran strict `verify_suite` over eight pilot side graphs (16,876–16,889 files and about 0.696–0.697 GiB per side). After 47:39 it remained CPU-active without an error. The termination diagnostic proves no formal output directory/Player existed and no child remained. This is an operational verifier scalability blocker, not a failed performance observation.

### Retention checkpoint

The authenticated pre-cleanup checkpoint is `Docs/AssemblyShadow/History/M07R/H1/local-validation-20260921-authority6913-v04-boundary/`. It includes the complete bounded raw evidence archive, readable graph/Player/provenance/restoration/map/preregistration/pilot/formal-blocker receipts, and a SHA-256 manifest. The 21 MiB old-Player receipt contains exact before/after hashes for the complete two-graph/four-Player immutable input inventory. No cleanup was performed.

H1 remains `InProgress`; the last independent M08 remains `FAIL`; `humanGatePassed=false`; `mayEnterR02=false`. `WEB_TO_LOCAL.md`, protected pins, source/runtime verification, and R02 were not modified.
