# Local Validation report

## Current run — 2026-09-17 consolidated handoff

### Exit

**Local Validation → Primary Implementation**

Candidate checkout `9f7a293d9b57743016b8d76df6c1eeb1d89dfd83` on `codex/assembly-shadow-r01b-h1` was validated against implementation anchor `bbd836747b6d5c8a2d00041d77b2042ca3ccce1b`. Unity was 2022.3.62f2 targeting StandaloneOSX arm64. Reproduction tooling and every protected reproduction/native/package/IL2CPP/performance pin remained exact.

V00–V03 pass. V01 includes 986 passing Python tests with 28 skips, real Unity compilation, and affected NUnit fixtures. V02 proves fresh normal-cache Apple ON/Debug provenance and a 318,212,421-byte logical capture store. V03 produces the required six fresh provenance-bound Players. V04 candidate count passes 132/132; all eight unfixed reproduction observations are retained as six `UnexpectedAccepted` and two Debug nested `AssertAbort`.

Controlled M07 now passes its expected-failure contract completely. Real `ValidateCompilerInputs` succeeds, both required baseline-bound files mutate, post-validation split authority returns `M07PostValidationAuthorityVerifiedNotBuildAccepted`, the exact deliberate failure is reached, all three guarded files restore byte-for-byte, and post-recovery preflight passes.

The separate normal M07 run passes validation, post-validation authority, baseline resource generation, and Native-ON Player generation. It then fails at the next pin check because Player generation rewrites tracked `Assets/HybridCLRGenerate/link.xml`. The generated bytes were preserved before Local restored the exact pinned blob. The handoff forbids adding a fourth mutable path locally, so Native-OFF, structural fixtures, Editor replay, startup11, capacity, retained coverage, performance, V05, and M08 remain blocked.

### Validation results

| Step | Result | Empirical result |
| --- | --- | --- |
| V00 dual preflight | `Pass` | Candidate `SourceTargetVerifiedNotBuildAccepted`; reproduction split authority and protected pins pass. |
| V01 Python / Unity / NUnit | `Pass` | 986 passed, 28 skipped; candidate and reproduction Unity compilation pass; affected Editor tests pass. |
| V02 provenance / retention | `Pass` | Fresh ON/Debug strict native/schema-3 managed provenance; 451 observations, 318,212,421 logical bytes, 72,612,459 stored bytes. |
| V03 six-build set | `Pass` | Candidate four modes plus reproduction two modes pass strict binding and restoration. |
| V04 candidate count | `Pass` | 132/132. |
| V04 reproduction | `Pass` as reproduction evidence | Six `UnexpectedAccepted`, two `AssertAbort`; not candidate acceptance. |
| V04 controlled M07 | `PassExpectedFailure` | Exact controlled throw reached after successful authority; exact three-file recovery and post-recovery preflight pass. |
| V04 normal M07 | `Fail` | Resources and Native-ON Player pass; post-Player pin guard rejects generated `Assets/HybridCLRGenerate/link.xml`. |
| V04 remaining runtime | `Blocked / NotRun` | Native-OFF, structural fixtures/replay, startup11, capacity, coverage, performance. |
| V05 / M08 | `Blocked / NotRun` | No complete fresh chain; independent M08 not commissioned. |

Portable current evidence is under [local-validation-20260917-consolidation](../History/M07R/H1/local-validation-20260917-consolidation/README.md). Earlier accepted or failed evidence remains under its original identity and is not relabeled. The actionable issue is in [RETURN_TO_WEB.md](RETURN_TO_WEB.md).

H1 remains `InProgress`; last independent whole-chain M08 remains `FAIL`; `humanGatePassed=false`; `mayEnterR02=false`; R02 was not started.
